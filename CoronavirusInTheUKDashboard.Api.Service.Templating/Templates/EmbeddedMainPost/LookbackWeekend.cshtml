@using Microsoft.AspNetCore.Html
@using System.Globalization;
@using RazorLight
@using CoronavirusInTheUKDashboard.Api.Service.Templating;

@inherits TemplatePage<CoronavirusInTheUKDashboard.Api.Service.Models.Models.MainPostModel>

Testing pillar data is available with a one day delay. Scotland and Wales do not provide vaccine data on weekends. Here is yesterday’s data.

||**Daily**|**Cumulative**|
:--|--:|--:|
@foreach (var item in Model.LookbackWeekendNationalResult.Records)
{
<text>@item.Name (@item.Date.ToString("d MMMM", CultureInfo.CreateSpecificCulture("en-US")) - @Enum.GetName(typeof(DayOfWeek), @item.Date.DayOfWeek)) | @HelperFunctions.Format(@item.Daily) | @HelperFunctions.Format(@item.Cumulative)
</text>
}